import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const Color primary = Color(0xFF2E7D32); // Green
  static const Color primaryLight = Color(0xFFE8F5E9);

  // Status Colors
  static const Color present = Color(0xFF2E7D32); // Green
  static const Color standby = Color(0xFFF57C00); // Orange
  static const Color absent = Color(0xFFD32F2F); // Red
  static const Color info = Color(0xFF1976D2); // Blue

  static const Color standbyLight = Color(0xFFFFF3E0);
  static const Color absentLight = Color(0xFFFFEBEE);
  static const Color infoLight = Color(0xFFE3F2FD);

  // Backgrounds
  static const Color background = Color(0xFFF5F7F9);
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);
  
  // Dividers & Borders
  static const Color divider = Color(0xFFEEEEEE);
  static const Color border = Color(0xFFE0E0E0);
}
